apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "linke-calendar.fullname" . }}-config
  labels:
    {{- include "linke-calendar.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ toYaml .Values.config | indent 4 }}
